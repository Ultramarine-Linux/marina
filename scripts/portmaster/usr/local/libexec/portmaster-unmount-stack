#!/bin/sh
# Tear down the per-port overlay after the launcher exits.
set -u

port=${1:?missing port instance}
mountpoint -q /roms/ports && umount /roms/ports || true
rm -rf "/run/user/1000/portmaster/work/$port"
